- Use `vp` for installs and tooling. Do not use `npm`, `pnpm`, `yarn`, or direct `vite`/`vitest` CLIs.
- Import from `vite-plus` and `vite-plus/test`, not `vite` or `vitest`.
- Use `vp check --fix` to fix code.
- To match CI locally:
  - `vp check`
  - `vp test --coverage --coverage.thresholds.100`
